<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.tailwindcss.com"></script>
    <title><%= title %></title>
</head>
<body class="bg-lime-100">
    <div class="flex gap-1 mx-10 mt-6 bg-lime-100">
        
        <a href="/elections" class="text-purple-800 hover:text-purple-600">All Elections</a>
        <span>></span>
        <a href="/elections/<%= electionID %>/" class="text-purple-800 hover:text-purple-600">Election</a>
        <span>></span>
        <a href="/elections/<%= electionID %>/voters" class="text-purple-800 hover:text-purple-600">Voters</a>
    </div>
    <div class="relative flex flex-col justify-center min-h-screen overflow-hidden">
        <div
            class="w-full p-6 m-auto bg-white border-t border-purple-600 rounded shadow-lg shadow-purple-800/50 lg:max-w-md">
            <h1 class="text-3xl font-semibold text-center text-purple-700">VOTER ADD</h1>
            <div>
                <% if (messages && messages.error && messages.error.length > 0) { %>
                    <% for(var i=0; i<messages.error.length; i++) { %>
                        <p class="bg-purple-100 my-3 list-none rounded-md px-4 py-2"><%= messages.error[i] %></p>
                    <% } %>
                <% } %>
            </div>
            <form class="mt-6" action="/elections/<%= electionID %>/voters/create" method="post">
                <input type="hidden" name="_csrf" value="<%= csrfToken %>" />
                <div>
                    <label for="voterid" class="block text-sm text-gray-800">Enter the Voter ID </label>
                    <input  id="voterid" name="voterid"  type="voterid" placeholder="Voter ID" 
                        class="block w-full px-4 py-2 mt-2 text-purple-700 bg-white border rounded-md focus:border-purple-400 focus:ring-purple-300 focus:outline-none focus:ring focus:ring-opacity-40">
                </div>
                <div>
                    <label for="password" class="block text-sm text-gray-800">Enter the Voter ID </label>
                    <input  id="password" name="password"  type="password" placeholder="password" 
                        class="block w-full px-4 py-2 mt-2 text-purple-700 bg-white border rounded-md focus:border-purple-400 focus:ring-purple-300 focus:outline-none focus:ring focus:ring-opacity-40">
                </div>
                    <div class="mt-6">
                        <button type="submit"
                            class="w-full px-4 py-2 tracking-wide text-white transition-colors duration-200 transform bg-purple-700 rounded-md hover:bg-purple-600 focus:outline-none focus:bg-purple-600">
                            Add Voter
                        </button>
                    </div>
            </form>
            <p class="mt-8 text-xs font-light text-center text-gray-700"> Wanna Go Back <a href="/elections/<%= electionID %>/voters"
                    class="font-medium text-purple-600 hover:underline">Click here</a></p>
        </div>
    </div>
</body>
</html>